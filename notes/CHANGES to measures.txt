Edit distance

- Try to normalize the edit distance, so that it's a relationship between the response of the participant, and the worst response that they could give (e.g. the inverse of the stimulus)

- are these kinds of sum scores OK to use in a linear model?

- do the absolute (time) edit distance, also calculate error





binary/ternary is going to be isochrony + binary + ternary divided by all the interval ratios (we simply want to know how much structure people introduce)